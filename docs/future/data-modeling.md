# Data Modeling

## Custom identity allocation

Custom identity allocation policies wait until the default single-`int`
allocation is fully exercised in practice. A GUID identity policy is one
candidate; it is not yet implemented.

See [`../data-modeling.md`](../data-modeling.md) for identity keys and the
default allocation policy as they work today.
