{
  "name": "marrow",
  "displayName": "Marrow",
  "description": "Language support for Marrow (.mw), backed by the bundled marrow language server.",
  "version": "0.1.1",
  "license": "Apache-2.0",
  "publisher": "marrow-project",
  "icon": "icons/marrow-gallery.png",
  "engines": {
    "vscode": "^1.82.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:marrow"
  ],
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false
    },
    "virtualWorkspaces": {
      "supported": false
    }
  },
  "contributes": {
    "languages": [
      {
        "id": "marrow",
        "aliases": [
          "Marrow"
        ],
        "extensions": [
          ".mw"
        ],
        "configuration": "./language-configuration.json",
        "icon": {
          "light": "./icons/marrow.svg",
          "dark": "./icons/marrow.svg"
        }
      }
    ],
    "grammars": [
      {
        "language": "marrow",
        "scopeName": "source.marrow",
        "path": "./syntaxes/marrow.tmLanguage.json"
      }
    ],
    "commands": [
      {
        "command": "marrow.restartServer",
        "title": "Marrow: Restart Language Server"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "vscode:prepublish": "tsc -p ./",
    "package": "[ -x \"$MARROW_LSP\" ] || { echo 'set MARROW_LSP to the marrow-lsp binary to bundle' >&2; exit 1; }; mkdir -p server && cp \"$MARROW_LSP\" server/marrow-lsp && chmod 755 server/marrow-lsp && vsce package"
  },
  "dependencies": {
    "vscode-languageclient": "9.0.1"
  },
  "devDependencies": {
    "@types/vscode": "1.82.0",
    "@vscode/vsce": "3.6.2",
    "typescript": "5.9.3"
  }
}
