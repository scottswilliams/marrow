//! The admitted-subset agreement gate and the resource-value pins it rests on.

pub mod common;

#[path = "agreement_gate/admitted_subset.rs"]
mod admitted_subset;
#[path = "agreement_gate/resource_values.rs"]
mod resource_values;
