# crates/ — Agent Notes

The crates in `crates/` form the Marrow pipeline, source text to committed saved
data. Start at the map and crate table:
[docs/implementation/](../docs/implementation/README.md). Each crate carries its
own `AGENTS.md` with its local law; the nearest one wins when you work inside a
crate.
