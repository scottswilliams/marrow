// Layer: runtime (storeless tracer subset; differential vs the frozen prototype binary)
// Oracle: `marrow test` on both stacks; each asserted literal is shared with tests/optional_test.mw
// Replaces: the T01 manifest-indirected `marrow run` comparison
module optional_checks

test "oOrDefaultAbsent" {
    assert optional::orDefaultAbsent() == 5
}

test "oOrDefaultPresent" {
    assert optional::orDefaultPresent() == 7
}

test "oIfConstPresent" {
    assert optional::ifConstPresent() == 7
}

test "oIfConstAbsent" {
    assert optional::ifConstAbsent() == 0
}
