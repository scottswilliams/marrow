// Layer: runtime (storeless tracer subset; differential vs the frozen prototype binary)
// Oracle: `marrow test` on both stacks; each asserted literal is shared with tests/text_test.mw
module text_checks

test "tJoin3" {
    assert text::join3("a", "b", "c") == "abc"
}

test "tEqText" {
    assert text::eqText("x", "x") == true
}
