// Layer: runtime (storeless tracer subset; differential vs the frozen prototype binary)
// Oracle: `marrow test` on both stacks; each asserted literal is shared with tests/control_test.mw
module control_checks

test "cSumTo" {
    assert control::sumTo(5) == 15
}

test "cFirstEvenOver" {
    assert control::firstEvenOver(5) == 6
}

test "cSign" {
    assert control::sign(-3) == -1
}
