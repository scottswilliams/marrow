module books
resource Book at ^books(id: int)
    required price: int
    required priceCents: int
evolve
    transform Book.priceCents
        return old.price * 100
pub fn add(price: int): Id(^books)
    return nextId(^books)
