module books
resource Book
    required title: string
    blurb: string
store ^books(id: int): Book
evolve
    rename Book.subtitle -> Book.blurb
pub fn add(title: string): Id(^books)
    return nextId(^books)
